package model.computer.property.hardware;

import java.math.BigDecimal;
import model.computer.property.Property;

public class Camera extends Property {

  public Camera(String description, BigDecimal price) {
    super(description, price);
  }
}

package model.computer.property.hardware;

import java.math.BigDecimal;
import model.computer.property.Property;

public class Battery extends Property {

  public Battery(String description, BigDecimal price) {
    super(description, price);
  }
}

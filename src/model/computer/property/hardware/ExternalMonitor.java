package model.computer.property.hardware;

import java.math.BigDecimal;
import model.computer.property.Property;

public class ExternalMonitor extends Property {

  public ExternalMonitor(String description, BigDecimal price) {
    super(description, price);
  }
}

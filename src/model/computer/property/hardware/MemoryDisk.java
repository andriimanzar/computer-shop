package model.computer.property.hardware;

import java.math.BigDecimal;
import model.computer.property.Property;

public class MemoryDisk extends Property {

  public MemoryDisk(String description, BigDecimal price) {
    super(description, price);
  }
}

package model.computer.property.hardware;

import java.math.BigDecimal;
import model.computer.property.Property;

public class WifiCard extends Property {

  public WifiCard(String description, BigDecimal price) {
    super(description, price);
  }
}
